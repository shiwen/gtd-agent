import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Temporarily disable PWA for Turbopack compatibility
  // Will re-enable after next-pwa updates for Next.js 16
};

export default nextConfig;
